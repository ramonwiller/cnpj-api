services:

  api:
    user: "${UID:-1000}:${GID:-1000}"
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload
    volumes:
      - .:/app
    ports:
      - "8000:8000"

  postgres:
    ports:
      - "5432:5432"